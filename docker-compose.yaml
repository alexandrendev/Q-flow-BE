services:
  db:
    image: postgres
    container_name: qflow-postgres
    environment:
      POSTGRES_DB: qflow
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata: